(program
  (name "test-collect-cli-like-42")
  (version "0.1")

  (include "test_cli_like_module.weave")

  (entry main
    (params ())
    (returns Int32)
    (body
      ;; Both should work - namespace function and top-level function
      (let ns-result Int32 (io::namespace-fn))
      (let top-result String (top-level-fn))
      (if-stmt (&& (== ns-result 10) (string-eq top-result "test")) 42 1)
    )
  )
)

