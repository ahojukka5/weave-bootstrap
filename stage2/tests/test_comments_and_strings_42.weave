(program
  (name "test-comments-strings-42")
  (doc "Ensure tokenizer/parser handle comments, strings, and symbols.")

  ;; This comment should be skipped by the tokenizer.

  (entry main
    (doc "Declare a string literal and return 42.")
    (params)
    (returns Int32)
    (body
      (do
        (let msg String "hello;world")
        ;; another comment after a binding
        (return 42)
      ) ;; do
    ) ;; body
  ) ;; entry
) ;; program
